module.exports = {
  // Specifies the number of spaces per indentation level
  tabWidth: 2,
  // Use tabs instead of spaces for indentation
  useTabs: true,
  // Whether a semicolon is required at the end of statements
  semi: true,
  // Whether to use single quotes
  singleQuote: true,
  // When to change quoting of object properties, options: "as-needed", "consistent", "preserve"
  quoteProps: 'as-needed',
  // Trailing commas where valid in ES5 (objects, arrays, etc.)
  trailingComma: 'all',
  // Print spaces between brackets in object literals
  bracketSpacing: true,
  // Include parentheses around a sole arrow function parameter, options: "always" (e.g., (x) => x)
  arrowParens: 'always',
  // Specifies global whitespace sensitivity for HTML files, options: "css", "strict", "ignore"
  htmlWhitespaceSensitivity: 'ignore',
  // Indentation for script and style tags in Vue files
  vueIndentScriptAndStyle: true,
};
